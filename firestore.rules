rules_version = '2';
service cloud.firestore {
    // NUCLEAR OPTION REVERTED
    match /databases/{database}/documents {
        match /{document=**} {
            allow read, write: if false;
        }
    }
}
